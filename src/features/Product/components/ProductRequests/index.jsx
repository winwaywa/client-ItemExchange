import React from 'react';
import PropTypes from 'prop-types';

ProductRequests.propTypes = {};

function ProductRequests({ tabIndex }) {
    return <div>Danh sách các yêu cầu</div>;
}

export default ProductRequests;
